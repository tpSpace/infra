apiVersion: v1
kind: Secret
metadata:
  name: github-ssh-repo-secret
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
type: Opaque
stringData:
  type: git
  url: git@github.com:tpSpace/infra.git
  
  sshPrivateKey: |
    ${SSH_PRIVATE_KEY}
