apiVersion: batch/v1
kind: Job
metadata:
  name: prisma-seed
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: seed
          image: your-backend-image
          command: ["sh", "-c", "npx prisma migrate deploy && npm run seed"]
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: db-credentials
